import Vue from 'vue';
import template from './template.vue';

// export default Vue.component('base', template);

export default new Vue({
  el: '#app',
  data: {
    meassage: 'Vue.js integration'
  }
});
